# 팀 프로젝트 시작 키트 — 하네스 엔지니어링 1세트

> 기술 리드용. 새 레포에 그대로 적용 가능한 ①시작 체크리스트 ②CLAUDE.md 템플릿 ③settings.json 자동검증 훅.
> `[대괄호]`는 네 프로젝트 값으로 치환할 자리.

---

## ① 시작 체크리스트 HKJHKJHKHJK

### Phase 0 — 팀 합류 전 (혼자 레일 깔기)
- [ ] **아키텍처 골격**: 폴더 구조 + 의존 단방향 확정, 핵심 데이터 모델 정의
      → 검증: 도메인을 그림 1장 + 폴더 트리로 설명 가능?
- [ ] **한 명령 인프라**: 셋업 1줄 / 테스트 1줄 / 실행 1줄
      → 검증: 신규 인원이 README로 5분 내 실행 가능?
- [ ] **CLAUDE.md 작성** (아래 ② 템플릿, 60줄 내외)
      → 검증: 이 파일만 읽고 AI가 첫 PR 가능?
- [ ] **공유 하네스 설정 커밋**: `.claude/settings.json`(아래 ③) git 추적
      → 검증: 테스트 깨고 커밋 시도 → 막히는가?
- [ ] **골든 패스 1개**: 입력→처리→출력 작동하는 얇은 수직 슬라이스 완성
      → 검증: 이걸 복붙해 두 번째 기능 제작 가능?

### Phase 1 — 팀 합류 후
- [ ] **온보딩 시연**: 같은 도구·설정 설치 후 "규칙→작업→자동테스트→PR" 직접 시범
- [ ] **래칫 룰 합의**: 같은 실수 2번 = CLAUDE.md에 규칙 1줄 추가 PR
- [ ] **리뷰 게이트**: 머지 전 코드리뷰 + CI, AI 코드도 동일 통과 의무

### 가장 먼저 (딱 하나)
**Phase 0의 "한 명령 인프라" + "자동검증 훅".** 이 둘이면 추측으로 망가뜨리는 사고가 구조적으로 차단됨.

### 피할 함정
- ❌ 레일 전에 팀원 투입 → 스타일 제각각
- ❌ 첫날부터 멀티에이전트·복잡 자동화 → 과설계 (1~5번부터)
- ❌ CLAUDE.md 방치 → 죽은 문서. 실수마다 갱신해야 살아있는 하네스

---

## ② CLAUDE.md 템플릿

```markdown
# [프로젝트명] — AI 참조용

## 한 줄 요약
[무엇을 하는 프로젝트인지 한 문장]

## 핵심 명령어
- 셋업:  [예: uv sync]
- 테스트: [예: uv run pytest]
- 실행:  [예: uv run python main.py]
- 린트:  [예: ruff check .]

## 아키텍처 (의존 단방향)
[layer A]  ← 오케스트레이션
[layer B]  ← 비즈니스 로직
[layer C]  ← 도메인 모델/타입
규칙: 위→아래로만 의존. 역방향 import 금지.

## 코딩 규칙
- 새 기능은 [골든 패스: 경로/파일]를 본보기로 따라 만든다
- 좌표/경로 하드코딩 금지. [설정/상수 위치] 사용
- 함수는 짧게, 추측성 추상화 금지 (1회용 코드에 일반화 X)

## 반드시 알아야 할 함정
- [실수날 때마다 여기에 한 줄씩 추가 — 래칫]
- (예) [모듈 X]는 [조건]일 때 [증상] → [대응]

## 절대 하지 말 것
- 테스트 삭제/수정으로 통과시키기 금지
- [건드리면 안 되는 디렉터리/파일]
- 검증 없이 "통과했을 것" 추측 보고 금지 — 반드시 테스트 실행 결과로 보고
```

---

## ③ `.claude/settings.json` — 권한 + 자동검증 훅

> Claude Code용. 코드 수정 시 테스트를 **자동 강제** 실행 → AI 추측 차단.
> `[테스트 명령]`을 네 프로젝트 것으로 바꿔라.

```json
{
  "permissions": {
    "allow": [
      "Bash(uv run *)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git status)",
      "Bash(git diff *)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(git push --force*)",
      "Bash(git reset --hard*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "[테스트 명령]"
          }
        ]
      }
    ]
  }
}
```

**작동 방식**: AI가 파일을 수정(`Edit`/`Write`)할 때마다 `[테스트 명령]`이 자동 실행되고,
실패하면 결과가 AI에게 되먹임되어 스스로 고치게 된다. "테스트 돌려줘(부탁)"가 아니라
"안 돌리면 진행 못 함(강제)" — 이게 하네스 엔지니어링의 핵심.

> 주의: 테스트가 느리면(수십 초+) 매 수정마다 돌면 거슬린다. 그 경우
> 빠른 단위 테스트만 훅에 걸고, 전체 회귀는 커밋 전(PreToolUse: git commit)에 한 번만 돌려라.

---

## 이 키트를 받은 Claude에게 (다음 액션)
사용자는 새 팀 프로젝트를 시작하는 기술 리드다. 위 3종을 사용자의 실제 스택
(언어/패키지매니저/테스트도구/쓰는 AI도구)에 맞춰 `[대괄호]`를 채워 완성해줘.
먼저 물어볼 것: "언어/런타임? 테스트 명령? 팀이 쓸 AI 도구(Claude Code/Cursor/기타)?"