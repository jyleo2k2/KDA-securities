# 연금 코파일럿 배포 운영 가이드

> 최종 확인: 2026-07-26 KST
> 적용 범위: Vercel 프론트엔드 → Render FastAPI → Supabase
> 상태: **원격 배포 복구·골든패스 E2E 완료 / `zeta` alias 단일 운영**

이 문서는 원격 배포 주소, 환경변수 계약, 배포 순서와 검증 절차의 정본이다. API 키와 DB 연결 문자열 등 비밀값은 이 문서에 기록하지 않는다.

## 1. 사용 서비스와 고정 주소

| 구분 | 서비스·프로젝트 | 주소 | 현재 확인 상태 |
|---|---|---|---|
| 사용자 프론트 | Vercel `kda-securities-lzix` | `https://kda-securities-lzix-zeta.vercel.app` | 2026-07-26 실측: HTTP 200, 로그인 벽 없음. **운영·외부 공유는 이 주소로 단일 운영한다** |
| 프론트 기본 alias | Vercel `kda-securities-lzix` | `https://kda-securities-lzix-kda4.vercel.app` | 2026-07-26 실측: Vercel SSO로 **302 리다이렉트**. 운영에 쓰지 않는다 — §4 참조 |
| 백엔드 API | Render `KDA-securities` (`srv-d9hee07lk1mc73dqomf0`) | `https://kda-securities.onrender.com` | 2026-07-26 실측: `/health` 200, CORS preflight 200, openapi 경로 42개가 로컬 `main`과 일치 |
| 데이터·인증 | Supabase `KDA-securities` | 대시보드 환경변수로 주입 | URL·publishable/secret·DB 연결값은 문서에 기록하지 않음 |

Vercel의 별도 프로젝트 `kda-securities`는 저장소 루트 `.`을 Root Directory로 사용해 `vite: command not found`로 실패한 중복 설정이다. 운영 프론트는 `kda-securities-lzix` 하나로 통일하고, 중복 프로젝트는 Git 연결을 해제하거나 삭제한다.

### 폐기된 옛 API 주소

`https://pension-copilot-api.onrender.com`은 **운영 대상이 아니다.** 2026-07-25 실측에서 이 주소는 여전히 HTTP 200을 반환하지만, 2026-07-21 이전 코드가 떠 있는 별개 서비스다. `/openapi.json` 대조 결과 이미 삭제된 `/chat/demo/*`가 남아 있고 이후 추가된 `/me/rebalancing-reminder`·`/engine/pension-calculator/combined`·`/engine/strategy-planning-returns`가 없다.

살아 있으면서 옛 코드를 돌려주기 때문에 오진을 유발한다. 이 주소로 상태를 확인하지 않는다.

## 2. Vercel 설정

### 프로젝트 빌드

| 항목 | 값 |
|---|---|
| Project | `kda-securities-lzix` |
| Root Directory | `frontend` |
| Framework Preset | `Vite` |
| Install Command | `npm install` |
| Build Command | `npm run build` |
| Output Directory | `dist` |

### Production·Preview 환경변수

```text
VITE_API_BASE_URL=https://kda-securities.onrender.com
VITE_SUPABASE_URL=<Supabase project URL>
VITE_SUPABASE_PUBLISHABLE_KEY=<Supabase publishable key>
```

- `VITE_API_BASE_URL`은 비어 있으면 안 되며 후행 `/`는 생략한다.
- Vite의 `VITE_` 변수는 build 시 번들에 들어가므로 값 변경 뒤 Vercel을 반드시 다시 배포한다.
- `SUPABASE_SECRET_KEY`, `DATABASE_URL` 등 서버 비밀값은 Vercel에 넣지 않는다.
- Production을 외부에 공유하려면 Vercel Deployment Protection이 로그인 없이 접근 가능한 정책인지 확인한다.

## 3. Render 설정

운영 서비스명은 Render 대시보드 기준 `KDA-securities`(Service ID `srv-d9hee07lk1mc73dqomf0`), health check는 `/health`다. `render.yaml`의 `name: pension-copilot-api`는 초기 blueprint 값이며 실제 대시보드 서비스명과 다르다. 상태 확인은 반드시 위 표의 실제 주소로 한다. Render 대시보드에는 서버 비밀값과 함께 다음 CORS 값을 설정한다.

```text
CORS_ORIGINS=["https://kda-securities-lzix-kda4.vercel.app","https://kda-securities-lzix-zeta.vercel.app"]
```

- 사용자가 실제 접속하는 모든 안정 alias를 JSON 배열에 정확히 넣는다.
- 값은 **JSON 배열 문자열**이어야 한다. 쉼표 나열·단일 URL·빈 문자열은 pydantic 파싱에 실패해 서버가 기동조차 하지 못한다.
- 임시 Preview URL은 매번 바뀌므로 원격 골든패스는 위 Production alias에서 검증한다.
- `render.yaml`이 `autoDeploy: false`이므로 `main` push만으로는 코드가 갱신되지 않는다. 코드 변경은 **Manual Deploy → Deploy latest commit**으로 반영한다. 환경변수 저장에 따른 자동 재시작은 기존 빌드를 그대로 쓴다.
- 대시보드 상단의 배포 커밋 해시가 최신 `main` HEAD와 같은지 확인한다.
- 무료 인스턴스는 유휴 뒤 첫 요청이 느릴 수 있다. 2026-07-24 실측에서는 첫 health/API 확인에 약 54초가 걸렸다.
- 시연 전에는 `/health`를 한 번 호출해 인스턴스를 미리 깨운다.

### 3-1. LLM 환경변수 (Claude)

Claude 호출은 **백엔드에서만** 일어난다. 모델·키 관련 값은 Vercel에 넣지 않는다(브라우저에 노출되면 안 되고, 프론트가 읽지도 않는다). 아래 값은 모두 Render 대시보드 소관이다.

| 환경변수 | 기본값(`settings.py`) | 담당 호출 | 비고 |
|---|---|---|---|
| `ANTHROPIC_MODEL` | `claude-haiku-4-5` | 챗봇 내레이터, ETF 상품 특징 문구 | 사용자가 기다리는 실시간 경로 |
| `ANTHROPIC_TOPIC_GUARD_MODEL` | `claude-haiku-4-5` | 주제 이탈(`UNSUPPORTED`) 질문 재분류 | 짧은 구조화 값만 받음 |
| `NEWS_SUMMARY_MODEL` | `claude-sonnet-5` | 뉴스 기사 원문 3줄 요약 | 일 1회 배치 전용. **Haiku로 낮추지 않는다** |

모델 배정 원칙은 **사용자가 화면 앞에서 기다리는 호출은 Haiku, 아무도 기다리지 않는 배치는 Sonnet**이다.

- 내레이터가 Haiku인 근거는 비용뿐 아니라 품질 실측이다. 10문항 A/B(질문당 20회)에서 검증 통과율이 Haiku 60%(thinking 미사용) vs Sonnet 20%(adaptive thinking)였다. Haiku 계열은 `narrator.py`에서 thinking을 끄고 호출한다.
- 뉴스 요약이 Sonnet인 이유는 기사 원문을 읽고 사실을 압축하는 난이도가 높고, 결과가 DB에 저장돼 재사용되기 때문이다. 챗봇의 뉴스 답변은 저장된 요약을 결정론적으로 조립할 뿐 LLM을 다시 부르지 않는다.
- 부팅 프리워밍은 응답을 버리는 워밍업이라 서비스 모델과 무관하게 항상 `narrator.PREWARM_MODEL`(Haiku)로 나간다. `ANTHROPIC_MODEL`을 Sonnet으로 올려도 프리워밍 비용은 오르지 않는다.
- 위 세 값은 Render 대시보드에 **없으면 기본값이 적용된다.** 굳이 추가할 필요가 없고, 이미 추가돼 있다면 위 표와 일치하는지 확인한다.

#### 기능 토글

| 환경변수 | Render 값 | 껐을 때 동작 |
|---|---|---|
| `ENABLE_CLAUDE_NARRATION` | `true` (2026-07-26~) | 규칙 엔진의 결정론 답변이 그대로 나간다. 수치·판단·출처는 동일하고 문장만 딱딱해진다 |
| `ENABLE_CLAUDE_TOPIC_GUARD` | 미설정(기본 `false`) | 1차 룰이 `UNSUPPORTED`로 판정한 질문을 재분류 없이 그대로 처리한다 |
| `ENABLE_ETF_PRODUCT_FEATURE_GENERATION` | 미설정(기본 `true`) | 저장된 ETF 특징 문구만 사용한다 |

내레이션 온오프 판단 기준은 다음 세 가지다.

1. **비용** — `true`면 대상 질문마다 API 과금이 발생한다. `false`면 0원이다.
2. **지연** — 답변 뒤에 재서술 단계가 붙는다. 다만 SSE가 엔진 답변을 먼저 흘리고 이후 `narration_update`로 교체하므로 체감 지연(TTFA)은 완화돼 있다.
3. **예측 가능성** — `false`가 100% 결정론이다. 같은 질문에 항상 같은 답이 나온다.

적용 범위는 좁다. `NARRATABLE_INTENTS` 4종(계좌 규칙·포트폴리오·공시·연금세액) 중 출처가 붙은 응답만 대상이며, 안전 가드를 통과하지 못한 재서술은 폐기되고 원본 답변으로 되돌아간다(`narration_mode`가 `claude_verified`가 아닌 경우). ETF 테마·뉴스 답변은 토글과 무관하게 결정론이다.

`ENABLE_CLAUDE_NARRATION=true`로 운영하려면 `ANTHROPIC_API_KEY`가 Render에 설정돼 있어야 한다. 키가 없거나 비어 있으면 내레이터가 생성되지 않고 자동으로 결정론 답변으로 동작한다(서버는 정상 기동한다).

## 4. 2026-07-26 실측

### 해결된 항목

1. Render `KDA-securities`가 Live이고 `/health`는 HTTP 200이다. `/openapi.json` 경로 42개가 로컬 `main`(`607beca`)에서 만든 앱의 경로 수와 일치한다.
2. CORS preflight는 zeta·kda4 두 오리진 모두 HTTP 200이고 `access-control-allow-origin` 헤더를 정상 반환한다. Render `CORS_ORIGINS` 설정은 반영 완료다.
3. Vercel `zeta` 배포 번들(`assets/index-D1io7I4b.js`)의 API 주소가 `https://kda-securities.onrender.com`으로 교체됐다. 번들에 `pension-copilot-api` 문자열은 남아 있지 않다.
4. 신규 API 경로(`/me/rebalancing-reminder`·`/engine/pension-calculator/combined`·`/engine/strategy-planning-returns`)가 모두 존재하고, 삭제된 `/chat/demo/scenarios`는 없다. 옛 서비스가 아니라 최신 코드다.

따라서 **프론트 → 백엔드 연결 경로는 복구 완료**다. 2026-07-25 시점의 "옛 API 주소 호출" 항목은 해소됐다.

5. 운영 프론트를 `zeta` alias 단일 운영으로 확정했다. `kda4`는 Vercel SSO로 302를 반환하지만 운영 대상이 아니므로 그대로 둔다. **외부 공유·시연에는 항상 `zeta`를 쓴다.** Render `CORS_ORIGINS`에는 두 alias가 모두 들어 있어 단일 운영으로 바꿔도 동작에 영향이 없다.
6. Vercel 중복 프로젝트 `kda-securities`(Root Directory 오류로 `vite: command not found`)를 정리했다. 운영 프론트는 `kda-securities-lzix` 하나다.
7. Render 대시보드의 `ENABLE_CLAUDE_NARRATION`·`ANTHROPIC_API_KEY`를 확인했고, 로그인 이후 화면·챗봇 전체 골든패스 E2E를 브라우저에서 완료했다.

**결론: 원격 배포 복구와 골든패스 E2E는 완료다.** 이후 상태 확인은 §6 검증 명령으로 재현한다.

## 5. 복구·배포 순서

1. Vercel 운영 프로젝트를 `kda-securities-lzix`로 통일한다.
2. Vercel Production·Preview의 `VITE_API_BASE_URL`을 `https://kda-securities.onrender.com`으로 설정한다. (2026-07-26 번들에서 확인 완료)
3. Render `CORS_ORIGINS`에 두 Production alias를 설정한다. (2026-07-25 완료)
4. Render를 Manual Deploy로 재배포하고 `/health`와 CORS preflight를 확인한다. (2026-07-26 확인 완료)
5. Vercel을 최신 `main`으로 재배포하고, 배포 번들에서 API 주소 문자열을 직접 확인한다. (2026-07-26 확인 완료)
6. 브라우저에서 로그인 → 계좌 정보 확인 → 투자성향 → 홈 → 챗봇 → 계산기 → 전략 → User Pick 순으로 원격 골든패스를 검증한다. (2026-07-26 완료)
7. Render 대시보드에서 `ENABLE_CLAUDE_NARRATION=true`와 `ANTHROPIC_API_KEY`를 확인한다. `render.yaml` 값은 blueprint 기준이며 대시보드 값이 우선한다. (2026-07-26 완료)

화면이 옛 상태로 보이면 코드가 아니라 캐시를 먼저 의심한다. 이 앱은 PWA라 서비스 워커가 이전 번들을 잡고 있어, 재배포 뒤에도 일반 새로고침으로는 옛 화면이 그대로 뜬다. 하드 리프레시(Ctrl+Shift+R)로 확인한다.

## 6. 검증 명령

### Render 상태

```powershell
curl.exe -sS https://kda-securities.onrender.com/health
curl.exe -sS https://kda-securities.onrender.com/accounts/link-options
```

둘 다 HTTP 200이어야 한다.

### 배포 코드가 최신인지 확인

```powershell
curl.exe -sS https://kda-securities.onrender.com/openapi.json | `
  ConvertFrom-Json | ForEach-Object { $_.paths.PSObject.Properties.Name.Count }
```

대시보드의 배포 커밋 해시가 최신 `main` HEAD와 같은지도 함께 본다. 옛 서비스와 구분하는 가장 빠른 방법은 `/me/rebalancing-reminder` 경로 존재 여부다.

### CORS preflight

```powershell
curl.exe -sS -o NUL -D - -X OPTIONS `
  https://kda-securities.onrender.com/accounts/link-options `
  -H "Origin: https://kda-securities-lzix-zeta.vercel.app" `
  -H "Access-Control-Request-Method: GET" `
  -H "Access-Control-Request-Headers: content-type"
```

응답에 아래 헤더가 있어야 한다.

```text
access-control-allow-origin: https://kda-securities-lzix-zeta.vercel.app
```

### 프론트 확인

- 공유 주소가 Vercel 로그인 화면으로 이동하지 않는다. 리다이렉트 여부는 `curl.exe -sS -o NUL -D - <주소>`의 `HTTP/1.1 302`·`location: https://vercel.com/sso-api`로 판별한다(2026-07-26 기준 `kda4`가 이 상태).
- 계좌 정보 화면에 재시도 오류가 나타나지 않는다.
- 브라우저 Network에서 `/accounts/link-options` 요청 host가 `kda-securities.onrender.com`이다.
- API·SSE 요청에 CORS 오류가 없다.

배포 번들에서 직접 확인하려면 `index.html`이 참조하는 `/assets/index-*.js`를 내려받아 API 주소 문자열을 검색한다. `pension-copilot-api`가 남아 있으면 옛 환경변수로 빌드된 번들이다.

## 7. 완료 판정

다음 조건을 모두 실행으로 확인해야 원격 배포 골든패스를 완료로 바꾼다.

- [x] 최신 `main` Vercel Production 배포 `Ready`
- [x] Vercel 배포 번들의 API 주소가 `kda-securities.onrender.com` (2026-07-26)
- [x] Render health·계좌 정보 API HTTP 200 (2026-07-26)
- [x] Render 배포 코드가 최신 `main` (openapi 경로 42개 일치, 신규 경로 존재) (2026-07-26)
- [x] Production alias CORS preflight 통과 (2026-07-26)
- [ ] 로그인 이후 주요 화면·챗봇 전체 E2E 통과
- [ ] 다른 브라우저 또는 비로그인 사용자에게 Vercel Deployment Protection이 노출되지 않음 — `kda4`가 SSO 302
- [ ] Render 대시보드 `ENABLE_CLAUDE_NARRATION=true`·`ANTHROPIC_API_KEY` 설정 확인 후 재배포
- [x] 문서의 상태와 실제 대시보드 설정 일치 (2026-07-26)
