# 연금 코파일럿 — 발표용 다이어그램 모음

> 작성일: 2026-07-22 · 최종 대조: 2026-07-27(`main` `9502819`) · Mermaid 소스. GitHub/VS Code/Obsidian 등에서 렌더링되며, 슬라이드에는 렌더 이미지를 캡처해 사용한다.
> 근거: [아키텍처.md](../30_스펙/아키텍처.md)·[코드베이스_지도.md](../30_스펙/코드베이스_지도.md)의 실제 파일·엔드포인트에 맞춰 작성.

---

## 1. 시스템 아키텍처 (Tech Stack & 계층)

```mermaid
flowchart TB
    subgraph Client["프론트엔드 · React + TS + Vite PWA"]
        UI["골든패스 화면\n로그인 → 연동동의 → 투자정보 → 메인홈\n챗봇 · MY투자 · 타고객 · 세금계산"]
    end

    subgraph API["백엔드 · FastAPI"]
        direction TB
        R["REST / SSE 엔드포인트\n/chat/* /engine/* /market/* /macro/*\n/me/* /accounts/* /retrieval/*"]
        subgraph Chat["chat/ 오케스트레이션"]
            QP["query_planner\n안전 라우팅 + 8-intent"] --> SVC["service (오케스트레이터)"]
            SVC --> H["handlers/*"]
            H --> PRZ["presentation\n시각화·후속질문"]
            PRZ --> NAR["narrator + narration_guard\nClaude 서술(검증 통과만)"]
        end
        ENG["engine/ 순수 규칙엔진\nDecimal 결정론 · 의존성 0"]
        RAG["retrieval/ RAG\nBGE-M3 하이브리드"]
    end

    subgraph Data["데이터 · Supabase PostgreSQL + pgvector"]
        DB[("구조화 테이블 · RLS\nknowledge_chunks(vector 1024)\nengine_runs 감사")]
    end

    LLM["Anthropic Claude API\n(서술·Q&A 전용)"]

    UI -- "REST/JSON · SSE" --> R
    R --> Chat
    H --> ENG
    H --> RAG
    NAR -. "검증 답변만" .-> LLM
    RAG --> DB
    SVC --> DB
    ENG -. "계산 결과(수치)" .-> H

    classDef eng fill:#e8f0fe,stroke:#4285f4;
    classDef data fill:#e6f4ea,stroke:#34a853;
    class ENG eng
    class DB,Data data
```

핵심: 의존은 `frontend → FastAPI → engine/data` 단방향. 엔진은 DB·네트워크·LLM에 의존하지 않는 순수 함수이고, LLM은 엔진이 낸 수치를 서술만 한다.

---

## 2. 데이터 수집 파이프라인 (수집 → 적재 → 서비스)

```mermaid
flowchart LR
    subgraph Sources["외부 공식/메타 데이터"]
        FSS["FSS 통합연금포털"]
        KRX["KRX ETF·지수"]
        KIS["KIS 구성종목·수정주가"]
        KIND["KIND 현금분배·분배락"]
        DART["DART·법령"]
        NAVER["NAVER 검색 API(뉴스)"]
    end

    subgraph Ingest["ingestion/ 수집기"]
        CLIENT["*_client.py\nHTTP + 응답검증\n(API키 미노출)"]
        CLI["수집 CLI\nraw 저장·SHA-256·skip·재시도"]
    end

    RAW[("파일캐시\ndata/raw · data/cache")]
    RUNS[("ingestion_runs\n호출·건수·상태")]

    subgraph Report["리포트 빌더 *_report.py"]
        CLS["ETF 분류·적격성"]
        MEV["시장근거(위험지표)"]
        RET["총수익·비용(분배 재투자)"]
        PORT["교육용 포트폴리오 120개"]
    end

    SUPA[("Supabase\netf_universe_products\netf_return_histories\n공시·뉴스·RAG")]

    Sources --> CLIENT --> CLI --> RAW
    CLI --> RUNS
    RAW --> Report --> SUPA
    NAVER --> NEWS["뉴스: 전문추출 → Claude 3줄요약\n(원문 숫자의 부분집합만 허용)"] --> SUPA
```

---

## 3. 모델 = 규칙 엔진 + 검증 게이트 위의 LLM (챗봇 1턴)

```mermaid
sequenceDiagram
    participant U as 사용자
    participant F as Frontend (useChatStream)
    participant A as FastAPI /chat/stream (SSE)
    participant P as query_planner (안전 라우팅)
    participant E as engine/ (규칙 엔진)
    participant R as retrieval/ (RAG)
    participant G as verify_evidence_links (검증 게이트)
    participant N as narrator + guard (Claude)

    U->>F: 질문 입력
    F->>A: POST (Bearer + Idempotency-Key)
    A->>P: 민감정보·주문·예측 차단 후 intent 분류
    P->>E: 수치 계산 (Decimal 결정론)
    P->>R: 근거 문서 검색 (BGE-M3 하이브리드)
    E-->>G: 수치 결과
    R-->>G: 출처·근거
    G->>G: 답변 속 모든 숫자를 NumericEvidence와 대조
    Note over G: 근거 없는 숫자 → 응답 생성 실패
    G-->>A: 검증된 결정론 답변
    A-->>F: answer_delta (엔진 답변 선전송)
    G->>N: 검증 통과분만 재서술 요청
    N->>N: 숫자·위험주장 가드 통과 확인
    N-->>A: narration_update (실패 시 원문 폴백)
    A-->>F: narration_update → response
    F-->>U: 출처 칩 + 서술
```

---

## 4. 교육용 포트폴리오 엔진 파이프라인 (성향 입력 → 포트폴리오)

```mermaid
flowchart TB
    IN["입력\n성향 · 나이 · 은퇴연령 · 계좌유형"] --> TA["목표배분 계산\nPROFILE_POLICY → 나이·은퇴 보정\n→ 계좌 한도 캡(DC·IRP 70% / 연금저축 90%)\n→ 손실감내율 이분탐색"]
    TA --> SL["5슬리브 배분\ncore_equity · real_assets · tactical\n· fixed_income · cash"]
    SL --> SC["후보 스코어링\n비용20·유동성25·규모20·NAV15·추적오차15·이력5\n(과거수익률은 랭킹 미사용)\n상관계수 0.75 초과·동일권역 페널티"]
    SC --> RK["포트폴리오 위험\n변동성·다운사이드·MDD·VaR95\n+ 스트레스 3종"]
    RK --> CMA["계획수익률\nCMA 앵커 + 불확실성 할인\n(예측 아님, 연 1회 검토)"]
    CMA --> OUT["포트폴리오 조립 + 리밸런싱 가이드\n(목표 이탈 5%p, 매도지시 미생성)"]

    GATE{"DC·IRP 70% 한도 위반?\n슬리브 누락?"}
    OUT --> GATE
    GATE -- "위반 시 exit 1 (CI 강제)" --> FAIL["빌드 실패"]
    GATE -- "0건" --> PASS["120개 포트폴리오 확정"]
```

---

## 5. 골든패스 유저플로우

```mermaid
flowchart LR
    L["로그인\n(대표고객 1~5 중 선택)"] --> C["계좌 연동 동의\n표시 전용·자동매매 미발생"]
    C --> I["투자정보 입력"] --> P["투자성향 결과\n(안정형~공격투자형 5단계)"]
    P --> M["메인 홈\n총자산·계좌·자산군 비중·납입현황"]
    M --> CHAT["챗봇\n용어·상품·전략·세금 Q&A"]
    M --> MY["MY투자·전략비교\n진단 → 전략 → 시뮬레이션 초안"]
    M --> OTH["타 고객 포트폴리오\n로그인 고객 제외 5명 동적"]
    M --> TAX["세금계산\n16.5/13.2% · 600/900만원 한도"]
    CHAT -. "화면 맥락 넘김" .-> MY
    OTH -. "차이보기 → 규칙검사 → 적용 초안" .-> MY
```

---

## 6. API 엔드포인트 맵 (2026-07-27 실제 라우트 47개 기준)

| 그룹 | 메서드 · 경로 | 인증 | 역할 |
|---|---|---|---|
| system | `GET /` · `GET /health` | 공개 | 헬스체크 |
| engine | `POST /engine/risk-cap` | 공개 | 위험자산 70% 한도 판정 |
| engine | `POST /engine/risk-cap/audited` | 인증 | 같은 판정 + 규칙 버전 대조·`run_id` 감사 저장 |
| engine | `POST /engine/profile` · `/diagnostics` · `/aggregation` · `/allocation-example` | 공개 | 성향·진단·집계·배분예시 |
| engine | `POST /engine/pension-calculator` · `/pension-calculator/combined` · `/pension-calculator/portfolio-cma` | 공개 | 단일·계좌 합산 적립 시계열, 보유 포트폴리오 기반 CMA 계획수익률 |
| engine | `POST /engine/pension-tax-credit` · `/non-pension-withdrawal-estimate` | 공개 | 세액공제·연금외수령 추정 |
| engine | `POST /engine/etf-planning-return` · `/etf-planning-assessment` · `/educational-portfolio` | 공개 | ETF 계획수익률·평가·포트폴리오 예시 |
| engine | `GET /engine/strategy-planning-returns` | 공개 | 전략 5종 계획수익률 표시값 |
| engine | `GET /engine/mock-scenario/{scenario_code}` | 공개 | 목시나리오 엔진 평가 |
| chat | `POST /chat/stream` | 인증 | SSE 대화(Idempotency-Key 필수) |
| chat | `GET /chat/capabilities` · `/scenarios` · `/heroes` | 인증 | 기능·시나리오·대표고객 메타 |
| chat | `GET /chat/cards` | 공개 | 추천/후속질문 정적 카탈로그 |
| chat | `GET /chat/rebalancing-profile` | 인증 | 저장 성향·보유내역 기반 리밸런싱 점검 입력 |
| chat | `GET /chat/sessions` · `DELETE /chat/sessions` · `DELETE /chat/sessions/{id}` · `GET /chat/sessions/{id}/messages` | 인증 | 세션 목록·전체/개별 삭제·메시지 |
| chat | `GET /me/pension-context` | 인증 | 로그인 사용자 연금 컨텍스트 |
| accounts | `GET /accounts/link-options` | 공개 | 계좌 3종 정적 링크 메타(표시 전용) |
| accounts | `GET /me/pension-accounts` | 인증 | 사용자 연금계좌 스냅샷 |
| profile | `POST /me/investment-profile` · `GET /me/investment-profile` | 인증 | 투자성향 저장/조회(24개월 유효) |
| reminder | `GET /me/rebalancing-reminder` · `PUT` · `POST /me/rebalancing-reminder/complete` | 인증 | 리밸런싱 점검 주기 설정·조회·완료 기록(`review_available` 포함) |
| market | `GET /market/etfs` · `/market/etfs/{isu_code}/volume-history` · `/distribution-events` | 공개 | 전체 ETF 거래량·종목별 이력·분배금 이벤트 |
| macro | `GET /macro/evidence` · `/macro/analog-regimes` · `POST /macro/analog-regimes/etf-outcomes` | 공개 | 거시근거·과거 유사국면·ETF 사후성과 |
| retrieval | `GET /retrieval/knowledge` · `/retrieval/news` | 공개 | RAG 검색·뉴스 목록 |
| disclosures | `GET /disclosures/pension-savings` · `/retirement` | 공개 | FSS 공시 최신분기 |
| benchmark | `GET /benchmark/summary` | 공개 | 익명 벤치마크 집계 |

> 전체 스키마는 서버 실행 후 `http://127.0.0.1:8000/docs`(OpenAPI)에서 확인.
