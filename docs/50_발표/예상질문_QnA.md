# 발표 예상질문 Q&A

> 작성 기준: 2026-07-27 코드·문서 상태. 발표자가 답변을 외우기보다 **결론 → 선택 이유 → 근거 → 한계** 순서로 설명할 수 있도록 정리했다.
>
> `🔴` 표시는 심사위원이 파고들 가능성이 높거나 현재 구현 범위를 정확히 밝혀야 하는 질문이다. 수치 실측은 측정 환경과 기준일을 함께 말하고, 비교 실험이 없는 기술은 우월성을 주장하지 않는다.

## 1. 질문 파트와 대응 전략

| 파트 | 주 질문자 | 확인하려는 역량 | 발표에서 먼저 보여줄 근거 |
|---|---|---|---|
| 기술 스택 선정 이유 | 심사위원 | 요구사항에 맞춘 선택과 트레이드오프 | 규칙 엔진·RAG·인증을 한 서비스 경계로 묶은 구조 |
| 아키텍처·역할 분리 | 심사위원 | LLM 통제, 책임 분리, 장애 시 동작 | 규칙 엔진 계산 → 검증된 답변 → LLM 서술 교체 흐름 |
| 금융 도메인 정확성 | 금융 실무 심사위원 | 법규·계좌 차이를 코드로 정확히 반영했는지 | DC·IRP와 연금저축 규칙 분리 및 불변식 테스트 |
| 데이터 신뢰성·재현성 | 심사위원 | 외부 데이터의 출처와 재수집 가능성 | 원본·요청 파라미터·기준일·해시·정규화 결과 연결 |
| 구현 디테일 | 동료 수강생 | 인증, 스트리밍, 상태, 테스트의 실제 구현 | RLS·소유자 조건·사용자 전환 초기화의 3중 격리 |
| 한계·운영 전환 | 양쪽 | 과장 없이 다음 과제를 판단하는 태도 | 미구현·미검증 항목과 보완 우선순위 |

발표 흐름은 p.14 아키텍처에서 질문의 중심을 잡고, 다음 슬라이드에서 프리워밍 트러블슈팅을 수치로 보여주는 구성이 가장 방어력이 높다. Q&A에서는 “왜 그 기술인가”를 물어도 가능한 한 **LLM이 계산하지 못하도록 만든 구조**와 **계좌 규칙 분리**로 연결한다.

## 2. 기술 스택 선정 이유

### Q1. 왜 백엔드로 FastAPI를 선택했나요? Django나 Flask와 비교했나요?

**30초 답변:** 이 프로젝트는 인증, 외부 데이터 수집, Python 규칙 엔진, RAG, 챗 응답 스트리밍을 하나의 API 경계에서 조합해야 했습니다. 계산 로직이 Python에 있기 때문에 같은 런타임에서 Pydantic 계약과 비동기 API를 연결할 수 있는 FastAPI가 요구사항에 잘 맞았습니다. 다만 Django·Flask와 정량 비교한 기록은 없어서 “가장 우수하다”가 아니라 “현재 구조에 적합했다”고 설명하는 것이 정확합니다.

**꼬리질문 대비:** Django의 관리자·ORM 생태계보다 독립 규칙 엔진과 명시적 API 계약을 우선했습니다. Flask 대비 자동 OpenAPI와 Pydantic 검증을 활용하기 쉬웠습니다. 이 비교는 구현 특성에 따른 설명이며 별도 벤치마크 결과는 아닙니다.

**근거:** `docs/30_스펙/아키텍처.md:29-34`, `backend/app/main.py`

### Q2. 왜 Supabase PostgreSQL을 선택했나요? Firebase와 비교한 이유는 무엇인가요?

**30초 답변:** 사용자별 계좌·대화·투자성향처럼 관계와 소유권이 분명한 데이터가 중심이라 PostgreSQL이 맞았고, Supabase Auth와 RLS를 결합해 타 사용자 행 접근을 DB 계층에서도 차단할 수 있었습니다. Firebase와의 정량 비교 자료는 없으며, 핵심 선정 이유는 관계형 모델과 행 단위 권한을 한 플랫폼에서 운영할 수 있다는 점입니다.

**트레이드오프:** 서비스 역할 키의 권한이 크므로 브라우저에 노출하지 않고, 노출 스키마는 `GRANT`와 RLS를 각각 검증해야 합니다.

**근거:** `docs/30_스펙/아키텍처.md:170-178`, `supabase/AGENTS.md:47-53`

### Q3. 🔴 왜 별도 벡터 DB 대신 pgvector를 사용했나요?

**30초 답변:** 2026-07-19 검증 기준 승인 지식은 문서 10개·총 청크 45개(활성 41·비활성 4)로 작고, 전문검색과 벡터검색 결과를 RRF로 합치는 구조입니다. 이 규모에서는 인증·메타데이터·벡터를 PostgreSQL 안에서 함께 관리하는 편이 운영 경계를 단순화합니다. HNSW 인덱스를 사용하지만, 코퍼스와 트래픽이 크게 늘면 Pinecone 같은 별도 검색 계층을 재검토해야 합니다.

**말하면 안 되는 답:** 별도 벡터 DB보다 항상 빠르거나 저렴하다는 주장. 해당 비교 실험은 없다.

**근거:** `docs/30_스펙/아키텍처.md:18`, `docs/30_스펙/아키텍처.md:77`, `docs/30_스펙/아키텍처.md:112`

### Q4. 🔴 왜 임베딩 모델로 BGE-M3를 선택했나요?

**30초 답변:** 1024차원 임베딩을 로컬에서 생성해 호출 비용과 외부 API 의존을 줄이고, 모델을 사용할 수 없는 환경에서는 전문검색으로 폴백하도록 구성했습니다. 모델 크기가 약 2GB이므로 프로세스마다 한 번만 로드합니다. 한국어 성능이 다른 모델보다 우수하다는 자체 벤치마크는 없으므로 그 이유를 선정 근거로 주장하지 않습니다.

**트레이드오프:** 첫 로드 비용과 메모리 사용량이 크며, 검색 품질 평가는 별도 질의셋으로 계속 측정해야 한다.

**근거:** `backend/app/ingestion/embeddings.py:3-4`, `backend/app/ingestion/embeddings.py:90`

### Q5. 왜 Claude와 pydantic-ai를 사용했고, 모델을 어떻게 나눴나요?

**30초 답변:** LLM은 계산자가 아니라 엔진 결과를 설명하는 역할로 제한했고, pydantic-ai의 구조화된 도구·응답 계약으로 그 경계를 구현했습니다. 모델은 감으로 고르지 않고 10문항을 각 20회 실행해 검증 통과율을 비교했습니다. Haiku가 60%, Sonnet adaptive thinking이 20%였기 때문에 실시간 내레이션은 Haiku, 일 1회 배치 요약은 Sonnet으로 분리했습니다. 이 결과는 해당 과제·프롬프트에서의 내부 실측이며 GPT나 Gemini와 비교한 결과는 아닙니다.

**추가 수치:** thinking ON은 약 7.2초, OFF는 약 3.6초였다.

**근거:** `docs/50_발표/발표_개요.md:76`, `docs/30_스펙/배포_운영_가이드.md:74-81`, `backend/app/chat/narrator.py:255-270`

### Q6. 왜 React + TypeScript + Vite PWA인가요? 네이티브 앱과 비교했나요?

**30초 답변:** 발표와 검증 단계에서 하나의 웹 코드베이스로 모바일 설치 경험, LAN 단말 테스트, 빠른 배포를 확보하려고 PWA로 구성했습니다. TypeScript는 화면과 API 계약의 정적 오류를 줄이고 Vite는 개발·빌드 경로를 단순화했습니다. 네이티브 앱과의 정량 비교는 없으며, 푸시·백그라운드·기기 API가 핵심이 되면 선택을 다시 검토해야 합니다.

**보안·캐시 포인트:** 인증·SSE 응답은 서비스워커 캐시에 남기지 않는 `no-store` 정책을 적용했다.

**근거:** `frontend/vite.config.ts:9`, `frontend/vite.config.ts:16-36`, `frontend/src/pwa/cachePolicy.ts:2-16`

### Q7. 왜 계산을 Python 규칙 엔진으로 분리했나요?

**30초 답변:** 연금계좌 한도와 세액 계산은 같은 입력에 같은 결과가 나와야 하고, 근거를 테스트로 고정할 수 있어야 합니다. 그래서 `Decimal` 기반 규칙 엔진이 계산·판단을 맡고 LLM은 결과를 바꾸지 못하게 했습니다. 이는 편의보다 금소법상 부당권유 위험과 생성형 모델의 수치 변형 가능성을 줄이기 위한 선택입니다.

**근거:** `docs/50_발표/발표_개요.md:66`, `backend/app/engine/AGENTS.md:20-21`

## 2-1. 비용·속도 측면의 선정 근거

> 외부 가격 확인일: 2026-07-27. 가격과 무료 제공량은 바뀔 수 있으므로 발표 직전에 공식 페이지를 다시 확인한다. 외부 공식 자료는 기술의 일반 특성, 프로젝트 코드·실측은 우리 선택의 실제 근거로 구분한다.

### 근거 강도 요약

| 스택 | 비용 근거 | 속도 근거 | 발표 근거 강도 |
|---|---|---|---|
| Claude Haiku·Sonnet 역할 분리 | 실시간 다빈도 경로를 저가 Haiku로 제한 | 모델·thinking·프리워밍 내부 실측 | **매우 강함** |
| FastAPI | Python 엔진과 API를 같은 런타임에 두어 중복 구현 경계 축소 | 공식 프레임워크 특성은 있으나 프로젝트 부하테스트 없음 | **중간 이상** |
| Supabase PostgreSQL | DB·Auth·RLS·Storage·Vector 운영 지점 통합 | 원격 Auth/RLS·DB E2E는 있으나 대안 대비 지연 측정 없음 | **중간** |
| pgvector | 별도 벡터 서비스 구독·동기화 경계 불필요 | HNSW 적용, 현재 작은 코퍼스에서는 전후 실측 없음 | **비용 중간·속도 약함** |
| BGE-M3 | 모델 API 호출료 없이 로컬 임베딩 가능 | 첫 로드·2GB 이상 메모리 비용, 운영 설치 상태 제약 | **조건부** |
| React·Vite PWA | 웹과 모바일 설치 경험을 한 코드베이스에서 제공 | Vite HMR은 개발 반복 속도 근거, 사용자 응답속도 근거는 아님 | **개발비 측면 중간** |
| Vercel·Render 배포 | 낮은 초기 인프라 비용으로 시작 가능 | CDN 이점과 Render 무료 인스턴스 콜드 스타트가 공존 | **비용 강함·속도 트레이드오프** |

### Claude: 비용과 속도 근거가 가장 강한 항목

현재 실시간 챗봇 내레이터·토픽가드는 `claude-haiku-4-5`, 일 1회 뉴스 요약은 `claude-sonnet-5`로 분리돼 있다.

- Anthropic 공식 가격 기준 Haiku 4.5는 입력 100만 토큰당 **$1**, 출력 100만 토큰당 **$5**다.
- Sonnet 5는 2026-08-31까지 입력 **$2**·출력 **$10**, 이후 입력 **$3**·출력 **$15**다.
- 동일 토큰을 사용한다고 단순 비교하면 Sonnet 5는 Haiku 4.5보다 현재 약 2배, 프로모션 종료 후 약 3배 비싸다.
- 실시간 다빈도 경로를 Haiku로 두고, 저빈도·고난도 뉴스 요약에만 Sonnet을 사용하는 구조는 호출 단가와 응답 지연을 함께 고려한 선택이다.

공식 근거: [Claude Haiku 4.5](https://www.anthropic.com/claude/haiku), [Claude Sonnet 5](https://www.anthropic.com/news/claude-sonnet-5)

아래는 실제 청구액이 아니라 **호출당 입력 1,000토큰·출력 250토큰**을 가정한 설명용 계산이다.

| 모델 | 1회 추정 비용 | 1,000회 추정 비용 |
|---|---:|---:|
| Haiku 4.5 | 약 $0.00225 | 약 $2.25 |
| Sonnet 5 프로모션 가격 | 약 $0.0045 | 약 $4.50 |
| Sonnet 5 정상 가격 | 약 $0.00675 | 약 $6.75 |

실제 비용은 한국어 토큰 수, 시스템 지시, 출력 길이, 재시도와 캐시 적중량에 따라 달라진다. 현재 애플리케이션은 실제 입력·출력·캐시 토큰과 비용을 집계하지 않으므로 월 절감액을 확정 수치로 말할 수 없다.

내부 속도 근거는 다음과 같다.

- 내레이터 콜드 첫 호출 약 **14초 → 프리워밍 후 약 4초**
- adaptive thinking ON 약 **7.2초 → OFF 약 3.6초**
- 공시 질의 연결 풀 적용 약 **1,200ms → 396ms**
- 시스템 지시와 도구 정의에 Anthropic 서버측 프롬프트 캐싱을 적용한다. 공식 안내상 Haiku 4.5는 프롬프트 캐싱으로 입력 비용을 최대 90% 줄일 수 있지만, 우리 캐시 적중 토큰은 미집계 상태다.

**발표 문장:** “사용자가 기다리는 실시간 내레이션은 속도와 호출 단가가 낮은 Haiku로 처리하고, 하루 한 번 수행하는 뉴스 요약만 Sonnet에 맡겼습니다. 프리워밍으로 첫 호출을 내부 개발 환경에서 약 14초에서 4초로 줄였습니다.”

**주의:** 뉴스 요약은 정기 실행이지만 Anthropic Message Batches API를 사용하지 않는다. 따라서 Batch API의 50% 할인은 현재 비용 절감 근거로 쓰지 않는다.

**프로젝트 근거:** `docs/30_스펙/아키텍처.md:220`, `docs/30_스펙/배포_운영_가이드.md:74-82`, `backend/app/chat/narrator.py:246-294`, `backend/app/ingestion/news_summarizer.py:84-99`

### FastAPI: 실행속도보다 개발비 절감 근거가 더 강함

FastAPI 공식 문서는 Uvicorn 기반 FastAPI가 Python 프레임워크 중 높은 처리성능을 보이고, 데이터 검증·직렬화·OpenAPI 문서를 함께 제공한다고 설명한다. 동시에 단순 벤치마크가 이런 추가 기능을 공정하게 비교하지 못할 수 있다고 경고한다. 공식 근거: [FastAPI Benchmarks](https://fastapi.tiangolo.com/benchmarks/)

우리 프로젝트에서 더 직접적인 근거는 규칙 엔진·수집기·RAG가 모두 Python이고, Pydantic 모델을 API 입력·출력 검증에도 사용한다는 점이다. 별도 언어 백엔드를 두면 계산 모델의 재작성·직렬화·계약 동기화 비용이 생긴다.

**발표 문장:** “핵심 계산 엔진과 API를 같은 Python 런타임에 두어 모델 재작성과 직렬화 경계를 줄였고, FastAPI의 Pydantic 검증·OpenAPI·SSE를 활용해 개발 생산성과 API 성능을 함께 확보했습니다.”

**주의:** 프로젝트 자체 부하테스트가 없으므로 Django·Flask보다 몇 배 빠르다고 말하지 않는다. 이 서비스의 체감 병목은 프레임워크보다 외부 LLM 호출과 무료 인스턴스 콜드 스타트에 가깝다.

### Supabase PostgreSQL·pgvector: 통합에 따른 비용 절감

Supabase 한 플랫폼에서 PostgreSQL, Auth, RLS, Storage, Data API와 pgvector를 운영할 수 있다. 공식 가격은 Free 월 $0, Pro 월 $25부터지만 실제 프로젝트 요금제는 저장소에서 확인되지 않으므로 현재 청구 금액으로 단정하지 않는다. 공식 근거: [Supabase Pricing](https://supabase.com/pricing)

비용 근거는 별도의 인증 서비스와 벡터 DB를 추가하지 않고 기존 PostgreSQL 안에서 사용자 데이터·검색 메타데이터·벡터를 함께 관리한다는 점이다. 이는 구조적 절감이며 Firebase·Pinecone 조합과 실제 견적을 비교한 결과는 아니다.

권한은 통합 비용과 별개로 두 계층을 검증한다.

- `GRANT`: Data API를 통해 테이블 자체에 접근할 수 있는지 제어
- RLS: 접근 가능한 테이블 안에서 어떤 사용자 행을 볼 수 있는지 제어

pgvector에는 HNSW 인덱스를 적용했다. Supabase는 데이터가 커졌을 때 순차 탐색의 자원 사용을 줄이기 위해 HNSW를 일반적으로 권장한다. 공식 근거: [Supabase Vector Indexes](https://supabase.com/docs/guides/ai/vector-indexes)

**발표 문장:** “현재 RAG 코퍼스가 작고 사용자·권한·검색 메타데이터가 이미 PostgreSQL에 있기 때문에 pgvector를 사용했습니다. 별도 벡터 서비스 구독과 데이터 동기화 경계를 추가하지 않고 전문검색과 벡터검색을 한 DB에서 결합했습니다.”

**주의:** 현재 문서 10개·청크 45개 규모에서 HNSW 전후 속도를 측정하지 않았다. “별도 운영비와 경계를 줄였다”는 주장과 달리 “검색이 크게 빨라졌다”는 주장은 아직 근거가 없다.

**프로젝트 근거:** `docs/30_스펙/아키텍처.md:112`, `docs/30_스펙/아키텍처.md:170-178`, `scripts/verify_auth_rls_e2e.py`

### BGE-M3: API 호출료 대신 로컬 연산비를 감수한 선택

BGE-M3가 포함된 FlagEmbedding은 MIT 라이선스로 공개돼 있고 다국어·dense·lexical·multi-vector 검색을 지원한다. 공식 근거: [FlagEmbedding](https://github.com/FlagOpen/FlagEmbedding)

로컬 임베딩은 외부 모델 API 호출료와 네트워크 의존을 없앨 수 있지만 무료 연산은 아니다. 모델 로드에 2GB 이상 메모리가 필요하고 첫 로드 지연, CPU·GPU 비용, 프로세스별 메모리 사용이 생긴다. 프로젝트는 모델을 프로세스당 한 번만 로드하고 최근 질의 벡터 512개를 LRU로 유지한다.

현재 Render 빌드는 `uv sync --frozen --no-dev`이고 `sentence-transformers`는 별도 `embeddings` 그룹이다. `render.yaml`만 기준으로 보면 운영 빌드에는 이 그룹이 설치되지 않으며, 코드는 미설치·실패 시 전문검색으로 폴백한다.

**발표 가능:** “로컬 적재·검증 환경에서 외부 임베딩 API 호출료를 피했고, 모델이 없는 환경에서도 전문검색으로 서비스가 유지됩니다.”

**발표 보류:** “현재 Render의 모든 RAG 요청이 BGE-M3 하이브리드 검색을 사용한다”, “BGE-M3로 운영 검색이 빨라졌다.” 배포 의존성과 원격 질의 경로를 다시 검증하기 전에는 말하지 않는다.

**프로젝트 근거:** `pyproject.toml:23-29`, `render.yaml:10-12`, `backend/app/ingestion/embeddings.py:1-10`, `backend/app/ingestion/embeddings.py:90-100`, `backend/app/retrieval/repository.py:124-136`

### React·Vite PWA·Vercel: 개발 반복과 배포 경계 절감

Vite는 개발 중 의존성을 미리 번들링하고 소스는 네이티브 ESM으로 제공하며, HMR로 변경 모듈만 갱신한다. 이는 사용자의 API 응답속도보다 개발자의 화면 반복 속도를 뒷받침하는 근거다. 공식 근거: [Why Vite](https://vite.dev/guide/why.html)

PWA를 사용하면 웹과 모바일 설치 경험을 하나의 React 코드베이스에서 제공할 수 있고, 정적 프론트와 API 서버를 분리할 수 있다. Vercel 공식 가격표에서 Hobby는 월 $0이며 글로벌 CDN을 포함하지만, 실제 프로젝트 요금제는 저장소에서 확인되지 않는다. 공식 근거: [Vercel Pricing](https://vercel.com/pricing)

**발표 문장:** “제한된 개발 기간에 iOS·Android 앱을 각각 만들기보다 React PWA 하나로 모바일 설치 경험을 제공했고, Vite HMR로 화면 개발 반복을 줄였습니다. 정적 프론트는 CDN에 배포해 API 서버와 부하 경계를 분리했습니다.”

**주의:** 개발 시간이 몇 퍼센트 줄었는지 측정하지 않았고, PWA가 네이티브 앱보다 항상 저렴하거나 빠르다고 일반화하지 않는다. 푸시·백그라운드·기기 API가 핵심이면 선택을 재검토해야 한다.

### Render 무료 인스턴스: 초기 비용과 콜드 스타트의 교환

`render.yaml`에는 `plan: free`가 명시돼 있어 Render API 서버가 무료 구성이라는 저장소 근거가 있다. Render 공식 문서에 따르면 무료 웹 서비스는 15분 동안 요청이 없으면 중지되고 다음 요청에서 다시 시작하며, 운영 서비스에는 권장되지 않는다. 공식 근거: [Render Free Web Services](https://render.com/docs/free)

**발표 문장:** “발표·검증 단계의 초기 인프라 비용을 낮추기 위해 Render 무료 인스턴스를 사용했습니다. 대신 유휴 후 콜드 스타트가 발생하므로 실제 운영 전환 시에는 상시 실행 인스턴스와 부하테스트가 필요합니다.”

프리워밍은 프로세스가 시작된 뒤 LLM·임베딩의 첫 호출 비용을 미리 흡수하지만, Render 인스턴스 자체가 중지됐다가 다시 시작하는 플랫폼 콜드 스타트까지 없애지는 못한다.

### 비용·속도 질문의 최종 답변 구조

1. **개발비:** “Python 규칙 엔진과 FastAPI를 같은 런타임에 두고, React PWA 하나로 모바일 경험까지 구현해 중복 개발 경계를 줄였습니다.”
2. **운영비:** “Auth·PostgreSQL·RLS·Vector를 Supabase에 통합하고, 실시간 LLM은 Haiku로 제한해 별도 서비스와 고가 모델 호출을 줄였습니다.”
3. **체감속도:** “스택 이름에 기대지 않고 직접 측정했습니다. 프리워밍으로 LLM 첫 호출을 약 14초에서 4초로, 연결 풀로 공시 질의를 약 1,200ms에서 396ms로 줄였습니다.”
4. **트레이드오프:** “무료 Render의 콜드 스타트, BGE-M3의 메모리 비용, 프로세스 로컬 캐시는 실제 운영 전환 시 재설계 대상입니다.”

### 후속 측정이 필요한 항목

- Claude 입력·출력·캐시 적중 토큰을 기록해 질문 유형별 실제 비용과 월 비용을 산출한다.
- RAG 전문검색과 하이브리드 검색의 동일 질의셋 p50·p95와 검색 적합도를 비교한다.
- Render 콜드·웜 상태의 첫 응답 시간과 프리워밍 완료 시간을 분리 측정한다.
- FastAPI 대표 사용자 흐름의 동시 접속·p95·오류율을 부하테스트한다.
- 현재 Vercel·Supabase 실제 요금제와 월 청구액은 대시보드 권한으로 확인한 뒤에만 발표 수치로 사용한다.

## 3. 아키텍처·역할 분리

### Q8. LLM이 실제로 숫자를 다시 계산하지 않는다는 것을 어떻게 보장하나요?

**30초 답변:** `evaluate_risk_cap()`과 `calculate_pension_tax_credit()` 같은 순수 함수가 계산한 결과만 도구 계약으로 LLM에 전달합니다. 내레이터가 숫자를 추가하거나 바꾸면 가드가 거부하고, 이미 생성된 결정론 답변을 유지합니다. 즉 프롬프트 지시만이 아니라 실행 순서와 사후 검증으로 막았습니다.

**근거:** `backend/app/engine/portfolio.py:47-101`, `backend/app/chat/tools.py:64-90`, `backend/app/chat/narrator.py:155-158`

### Q9. 질문의 의도 분류도 LLM이 하나요?

**30초 답변:** 1차 분류는 정규식·키워드 기반 `plan_question()`이 결정합니다. 개인정보, 주문 실행, 미래 예측, 개별 종목 요청 차단은 의도 판별보다 먼저 실행합니다. 규칙으로 분류되지 않은 질문만 LLM이 보조 분류하고, 그 결과도 다시 규칙 플래너를 통과시킵니다.

**근거:** `backend/app/chat/query_planner.py:896`, `backend/app/chat/query_planner.py:959`, `backend/app/chat/query_planner.py:1216`, `backend/app/chat/topic_guard.py:101`

### Q10. RAG와 규칙 엔진은 어떤 기준으로 나뉘나요?

**30초 답변:** “제도가 어떻게 되는가”처럼 공식 근거를 설명하는 질문은 RAG, “내 계좌에서 얼마인가”처럼 사용자 입력으로 계산하는 질문은 규칙 엔진이 담당합니다. 예를 들어 세액공제 제도 설명은 승인 문서를 검색하지만 실제 공제액은 엔진을 단일 진실 공급원으로 사용합니다.

**근거:** `docs/30_스펙/아키텍처.md`, `backend/app/chat/handlers/account_rules.py`, `backend/app/engine/pension_tax.py`

### Q11. 왜 WebSocket이 아니라 SSE를 사용했나요?

**30초 답변:** 현재 채팅 응답은 서버가 단계와 답변을 클라이언트로 보내는 단방향 스트림이면 충분해서 SSE로 단일화했습니다. WebSocket과의 별도 비교 ADR은 없으므로 복잡도나 성능 우위를 수치로 주장하지 않습니다.

**근거:** `backend/app/api/chat.py:319`

### Q12. 사용자는 LLM 답변이 늦을 때 무엇을 보나요?

**30초 답변:** 서버가 검증된 결정론 답변을 먼저 `answer_delta`로 전송하고, LLM 서술이 가드를 통과했을 때만 `narration_update`로 교체합니다. 따라서 LLM이 늦거나 실패해도 계산 결과를 먼저 볼 수 있고, 검증되지 않은 문장이 정답을 덮지 않습니다.

**근거:** `backend/app/api/chat.py:681`, `backend/app/api/chat.py:757`, `frontend/src/hooks/useChatStream.ts`

### Q13. 프리워밍으로 무엇이 개선됐나요?

**30초 답변:** BGE-M3와 내레이터처럼 첫 호출 비용이 큰 자원을 프로세스당 한 번 준비하고, 내레이션 캐시와 프롬프트 캐싱을 결합했습니다. 내부 개발 환경에서 내레이터 첫 호출은 약 14초에서 4초, 공시 질의는 약 1,200ms에서 396ms로 줄었습니다. 이 수치는 운영 SLA가 아니라 당시 개발 환경 실측입니다.

**트러블슈팅 서술:** 증상(첫 질문 지연) → 원인(모델·프롬프트 첫 준비 비용) → 조치(부팅 시 준비·캐시) → 결과(동일 조건 재측정) 순서로 말한다.

**근거:** `AGENTS.md`의 PR #51~#53·#54·#60 이력, `backend/app/chat/narrator.py`

### Q14. 동시 사용자가 늘어나도 확장 가능한가요?

**30초 답변:** 현재 부하테스트 결과는 없습니다. API와 DB 경계는 분리돼 있지만 내레이터·토픽가드·RAG 캐시가 프로세스 로컬이고 외부 LLM rate limit도 있어 수평 확장 시 캐시 일관성과 호출 제한이 병목 후보입니다. 다음 단계는 k6 또는 Locust로 p95 지연과 오류율을 측정한 뒤, 필요할 때 공유 캐시와 큐를 도입하는 것입니다.

**말하면 안 되는 답:** “대규모 트래픽을 견딘다.” 이를 입증할 측정이 없다.

## 4. 금융 도메인 정확성

### Q15. 🔴 적격 TDF나 디폴트옵션의 위험자산 한도 예외를 어떻게 판정하나요?

**30초 답변:** 상품명에서 TDF 여부를 추론하지 않습니다. 사업자 공식 목록의 `retirement_investment_limit_percent`가 70이 아닌 경우만 예외로 신뢰합니다. 근거 필드가 없으면 일반 위험자산 규칙을 적용해 보수적으로 실패합니다.

**근거:** `backend/app/engine/pension_eligibility.py:28-47`

### Q16. DC·IRP와 연금저축의 위험자산 규칙이 섞이지 않나요?

**30초 답변:** 계좌 타입별 모델과 평가 경로를 분리했습니다. 연금저축은 위험자산 총량 한도 필드가 `None`이고 결과도 `NOT_APPLICABLE`이며, 법정 예외를 임의 주입하면 Pydantic 검증에서 거부됩니다. 세 계좌 합산 값은 화면 표시용일 뿐 개별 계좌 규칙을 대체하지 않습니다.

**근거:** `backend/app/engine/models.py:130-141`, `backend/app/engine/models.py:174-187`, `backend/app/engine/aggregation.py`, `tests/test_engine.py:37-138`

### Q17. 🔴 연금계좌 연간 납입한도 1,800만원도 검증하나요?

**30초 답변:** 현재 엔진은 연금저축·IRP의 세액공제 한도 900만원 계산을 구현했지만, 연금계좌 합산 납입한도 1,800만원을 별도 차단하는 검증은 아직 없습니다. 발표에서는 둘을 혼동하지 않고, 1,800만원 검증은 후속 규칙으로 명시합니다.

**근거:** `backend/app/engine/pension_tax.py:533-555`

### Q18. 🔴 연금 수령세 계산에 이연퇴직소득과 종합과세도 포함되나요?

**30초 답변:** 현재 계산 범위에는 이연퇴직소득의 별도 과세와 종합과세 선택 구조가 포함되지 않습니다. 연간 수령액 1,500만원 초과 구간은 전액 16.5%라는 단순 가정을 적용합니다. 따라서 실제 세무 판단이 아니라 명시된 가정 아래 비교 결과로 설명해야 합니다.

**근거:** `backend/app/engine/pension_calculator.py:225-237`, `backend/app/engine/pension_calculator.py:311-358`

### Q19. 연금저축에서 위험자산 한도가 없으면 모든 ETF를 살 수 있다는 뜻인가요?

**30초 답변:** 아닙니다. 법정 위험자산 총량 한도가 없다는 것과 금융사가 해당 상품을 판매·주문 가능하게 열어둔 것은 별개입니다. 현재는 계좌 유형별 상품 적격성 규칙을 적용하지만 금융사별 실시간 주문 가능 목록까지 연동하지는 않았습니다.

**근거:** `backend/app/engine/pension_eligibility.py:59-101`

### Q20. ISA 만기자금 전환의 60일 요건을 검증하나요?

**30초 답변:** 추가 세액공제 계산 경로는 있지만, 현재 함수는 60일 이내 전환 여부를 호출자 입력으로 신뢰하고 날짜 자체를 검증하지 않습니다. 실제 계좌 연동 단계에서는 만기일·입금일을 받아 서버에서 판정하도록 보완해야 합니다.

**근거:** `backend/app/engine/pension_tax.py:169-210`

### Q21. 🔴 3%·7%·9% 복리 시나리오는 미래 수익 예측 아닌가요?

**30초 답변:** 특정 상품의 미래 수익률을 예측하는 값이 아니라 동일한 납입 조건을 비교하기 위한 사용자 선택 가정입니다. 결과에는 가정, 적용 기간, 변경 가능성을 함께 보여줘야 하며 “전망”이나 “예상 수익 확정”처럼 읽히는 표현은 피해야 합니다. 현재 일부 화면에 `전망` 표현이 남아 있어 발표 전 문구 검토가 필요합니다.

**근거:** `backend/app/engine/assumptions.py:35-53`, `frontend/src/pages/MainHomeScreen.tsx:197-201`

## 5. 데이터 신뢰성·재현성

### Q22. ETF 총수익률은 분배금을 어떻게 반영했나요?

**30초 답변:** KIND의 현금분배와 정확한 분배락일을 연결하고 분배락일 종가에 비용 없이 재투자한 것으로 계산합니다. 8,048건 중 8,029건은 정확한 분배락일, 원문이 부족한 19건은 지급기준일 대체 상태입니다. 세금과 매매비용은 반영하지 않았으므로 실제 투자자 수익률과 같다고 말하지 않습니다.

**근거:** `docs/30_스펙/ETF_총수익률_비용_마스터_계약.md:43-49`, `docs/30_스펙/아키텍처.md:214`

### Q23. 한투 수정주가를 사용했다는 사실을 사후에 입증할 수 있나요?

**30초 답변:** 요청 원본과 해시를 보존해 재현 경로는 남겼습니다. 다만 현재 897,330개 관측에서 `mod_yn=Y`와 명시적 조정 사유가 0건이어서 응답 플래그만으로 모든 수정 반영을 사후 입증할 수는 없습니다. 그래서 가격 급변만으로 분할·병합을 추정하지 않고, 명시 근거가 있을 때만 이벤트로 확정합니다.

**근거:** `docs/30_스펙/KIS_ETF_수정주가_수집_계약.md:60-67`

### Q24. ETF 후보 순위에 과거 수익률을 사용했나요?

**30초 답변:** 사용하지 않았습니다. 후보 순위는 일별 거래대금 중앙값 내림차순, 동률이면 총보수 오름차순으로 정합니다. 과거 총수익률은 설명 근거일 뿐 후보 순위나 장기 계획수익률 입력으로 쓰지 않도록 분리했습니다.

**근거:** `docs/30_스펙/ETF_테마_챗봇_계약.md:66-70`, `backend/app/engine/educational_portfolio.py:1705-1715`

### Q25. 외부 API 데이터가 바뀌면 같은 결과를 재현할 수 있나요?

**30초 답변:** 수집 실행마다 비밀키를 제외한 요청 파라미터, 기준일, 원본 경로, SHA-256, 정규화 결과를 연결하고 검증된 원본은 재사용합니다. 따라서 어떤 입력으로 어떤 결과가 만들어졌는지 추적할 수 있습니다. 다만 원천별 증빙 수준이 완전히 같지는 않으며 FSS는 원본 payload와 실행 기록 중심이라 원천별 해시 보존 수준을 계속 맞춰야 합니다.

**근거:** `docs/30_스펙/코드베이스_지도.md:131-138`, `docs/50_발표/발표_개요.md:48`

### Q26. 60일 위험지표로 장기 위험을 말할 수 있나요?

**30초 답변:** 장기 손실을 예측하는 지표로 사용하지 않습니다. 동일 기간의 변동성·낙폭을 비교하는 단기 관측값이며, 기준일과 관측창을 함께 표시합니다. 장기 의사결정에는 더 긴 표본과 국면별 검증이 필요합니다.

## 6. 구현 디테일 — 동료 수강생 질문

### Q27. JWT를 직접 디코딩해서 인증하나요?

**30초 답변:** 아닙니다. 전달된 토큰을 Supabase `/auth/v1/user`에 보내 원격 검증이 200으로 성공한 경우에만 응답의 사용자 ID를 신뢰합니다. 토큰 payload를 단순 디코딩해 사용하지 않습니다.

**근거:** `backend/app/auth.py:14`

### Q28. 사용자 데이터 격리는 어떻게 보장하나요?

**30초 답변:** 첫째, DB RLS가 `owner_id = auth.uid()`를 검사하고 UPDATE에는 `USING`과 `WITH CHECK`를 모두 둡니다. 둘째, 백엔드 쿼리도 `owner_id` 조건을 중복 적용합니다. 셋째, 프론트는 사용자가 바뀌면 로컬 상태와 저장값을 초기화하고 이전 비동기 응답을 generation 값으로 폐기합니다. 서버 전용 테이블은 브라우저 역할의 권한을 회수했습니다.

**근거:** `backend/app/chat/repository.py:411`, `frontend/src/App.tsx:134`, `supabase/migrations/`

### Q29. 액세스 토큰 만료 시 어떻게 처리하나요?

**30초 답변:** 일반 REST 요청은 401을 받으면 `refreshSession()`을 호출하고 같은 요청을 한 번만 재시도합니다. 무한 재시도를 막은 구조입니다. 다만 SSE POST에는 동일한 자동 갱신 경로가 없어 만료 시 재로그인 또는 스트림 재시도가 필요한 현재 한계가 있습니다.

**근거:** `frontend/src/api/client.ts:128`, `frontend/src/api/client.ts:223-247`

### Q30. SSE 이벤트 순서와 오류 처리는 어떻게 되나요?

**30초 답변:** 기본 순서는 `phase → answer_delta → 필요 시 narration_update → response`입니다. `answer_delta`는 LLM 토큰 조각이 아니라 검증된 전체 답변 한 건이고, 글자가 나타나는 효과는 프론트 표현입니다. 헤더 전송 후 저장이나 처리에 실패해도 연결을 조용히 끊지 않고 `error` 이벤트를 보냅니다. 클라이언트에는 45초 유휴 제한과 취소 기능이 있지만 자동 재연결은 없습니다.

**근거:** `backend/app/api/chat.py:319`, `backend/app/api/chat.py:327`, `backend/app/api/chat.py:681`, `backend/app/api/chat.py:757`, `frontend/src/api/client.ts:223-247`

### Q31. Redux, Zustand, React Query를 쓰지 않은 이유는 무엇인가요?

**30초 답변:** 현재는 상위 `App` 상태와 React 훅, `useChatStream` 같은 기능별 훅으로 상태를 관리합니다. 별도 전역 상태 라이브러리를 선택하지 않은 ADR은 없습니다. 화면 간 서버 상태 공유와 캐시 무효화가 복잡해지면 React Query나 경량 스토어 도입을 검토할 수 있지만, 현재 규모에서는 기존 구조로 동작을 검증했습니다.

**근거:** `frontend/src/App.tsx`, `frontend/src/hooks/useChatStream.ts`, `frontend/package.json`

### Q32. 백엔드와 프론트 타입은 어떻게 동기화하나요?

**30초 답변:** 서버는 Pydantic 모델, 프론트는 수동 TypeScript 타입과 `tsc --noEmit`으로 검사합니다. OpenAPI에서 타입을 자동 생성하거나 응답을 Zod처럼 런타임 검증하는 구조는 아직 없습니다. 따라서 계약 변경 시 양쪽 타입과 통합 테스트를 함께 수정해야 하는 비용이 남아 있습니다.

**근거:** `backend/app/chat/models.py`, `frontend/src/api/types.ts`, `frontend/package.json`

### Q33. 테스트는 어떤 계층으로 구성했나요?

**30초 답변:** 순수 엔진·플래너 단위 테스트, FastAPI `TestClient`와 dependency override를 이용한 경계 통합 테스트, migration SQL 정적 계약 테스트, 실제 Supabase Auth/RLS를 확인하는 별도 원격 E2E로 나눴습니다. 외부 HTTP와 저장소 경계는 테스트 대역으로 격리하고 프론트는 Vitest로 검증합니다.

**대표 불변식:** 무효 토큰 401, REST 401 갱신은 1회만, 공개 핵심 테이블 RLS 활성, 민감정보의 대화 저장 차단, 레버리지·인버스 상품의 세 계좌 부적격을 테스트로 고정했다.

**근거:** `tests/`, `frontend/src/**/*.test.tsx`, `scripts/verify_auth_rls_e2e.py`

### Q34. 뉴스 수집과 요약 실패는 어떻게 처리하나요?

**30초 답변:** 수집은 최대 24시간 범위·10페이지 안에서 수행하고, 정기 실행은 동시 실행 그룹으로 직렬화합니다. 요약은 구조화된 3문장 계약을 최대 3회 보정하며 끝까지 검증하지 못한 항목은 결과에서 제외합니다. 원문 전체를 서비스 DB에 저장하지 않고 요약과 본문 해시·출처를 남깁니다.

**근거:** `.github/workflows/`, `backend/app/ingestion/naver_news_repository.py`, `docs/30_스펙/아키텍처.md:130-132`

## 7. 한계·미구현·운영 전환

### Q35. 실제 금융사 계좌와 주문까지 연결되나요?

**30초 답변:** 현재는 마이데이터 연동을 전제로 계좌 동의·표시·분석 흐름을 구현했지만 실제 금융사 계좌 연결과 주문 실행은 포함하지 않습니다. 이 서비스는 정보를 제공하고 이용자가 최종 선택·주문하는 구조이며 일임 운용이 아닙니다.

**근거:** `README.md:167`, `docs/team/_공통_AI규칙.md:1`

### Q36. 운영 안정성을 어떤 수치로 보장하나요?

**30초 답변:** 기능·회귀 테스트는 폭넓게 수행했지만 k6·Locust·JMeter 기반 부하테스트, SLO, 알람, 장애 런북은 아직 없습니다. 따라서 테스트 개수와 운영 안정성을 같은 의미로 주장하지 않습니다. 다음 검증은 대표 사용자 흐름의 p50·p95 지연, 동시 접속, LLM 실패율과 폴백 성공률을 측정하는 것입니다.

### Q37. CI에서 모든 검사가 자동으로 강제되나요?

**30초 답변:** 워크플로는 있지만 2026-07-26부터 GitHub Actions 무료 실행 한도 소진으로 러너가 배정되지 않고 있습니다. 현재는 로컬 훅과 직접 실행한 백엔드·프론트 검증 결과를 근거로 말해야 합니다. “CI에서 강제된다”는 표현은 한도 복구 전까지 사용하지 않습니다.

**근거:** `docs/team/_공통_AI규칙.md:45-49`, `.github/workflows/ci.yml`

### Q38. LLM 가드가 모든 환각을 막나요?

**30초 답변:** 아닙니다. 숫자 추가·변형은 비교적 강하게 탐지하지만, 숫자가 없는 그럴듯한 서술 확장까지 완전히 검출하지는 못합니다. 그래서 LLM 실패 시 결정론 답변을 유지하고 승인 근거 범위를 제한했으며, 적대적 질의셋으로 재현 사례를 계속 추가하는 구조입니다.

**근거:** `backend/app/chat/narration_guard.py:333-340`, `docs/30_스펙/아키텍처.md:225`

### Q39. 현재 가장 먼저 보완할 기술 부채는 무엇인가요?

**30초 답변:** 금융 정확성 측면에서는 1,800만원 합산 납입한도와 ISA 60일 날짜 검증, 제품 문구에서는 복리 가정의 “전망” 표현, 운영 측면에서는 SSE 토큰 갱신·부하테스트·모니터링이 우선입니다. 코퍼스 확대 전에는 pgvector 교체보다 검색 평가셋을 먼저 만드는 것이 합리적입니다.

## 8. 발표자가 외울 핵심 답변 8개

1. **LLM은 계산하지 않는다.** Python 규칙 엔진이 계산하고 LLM은 검증된 결과만 설명한다.
2. **규칙은 계좌별로 분리했다.** DC·IRP의 70% 규칙을 연금저축에 적용하지 않는다.
3. **RAG와 엔진의 질문이 다르다.** 제도 설명은 RAG, 개인 수치 계산은 엔진이다.
4. **느리면 검증된 답부터 보여준다.** LLM 서술은 가드 통과 시에만 교체된다.
5. **데이터는 출처에서 결과까지 추적한다.** 요청·기준일·원본·해시·정규화 결과를 연결한다.
6. **과거 수익률로 ETF 순위를 만들지 않는다.** 거래대금 중앙값과 총보수로 순위를 정한다.
7. **비교하지 않은 기술은 우월하다고 말하지 않는다.** 요구사항 적합성과 감수한 비용을 함께 말한다.
8. **미구현 범위를 숨기지 않는다.** 실제 계좌 연결·주문, 부하테스트, 일부 세법 날짜 검증은 후속 과제다.

## 9. 발표 전 사실 점검 체크리스트

아래 항목은 현재 조사 결과와 기존 발표 자료가 어긋나므로 슬라이드 반영 전에 다시 확인한다.

- [ ] 백엔드 테스트: **1,566 passed, 1 skipped**로 표기한다.
- [ ] 프론트 테스트: **기능별 단독 실행과 30초 제한 전체 실행에서 209/209 통과**, 기본 5초 제한에서는 대형 화면 테스트가 간헐적으로 초과한다고 구분한다.
- [ ] ETF 총수익 이력: **898,190행**, 기준일 **2026-07-23(v4)**로 표기한다.
- [ ] ETF 상품: **2,507행(DC 823·IRP 823·연금저축 861)**로 표기한다.
- [ ] 현재 API 경로: **49개**, ERD 테이블: **54개**로 표기한다.
- [ ] `발표_개요.md`의 981/53, 217,833 수치와 `기능정의서.html`, `ERD.md`, `다이어그램.md`의 이전 수치를 최신 근거와 대조한다.
- [ ] “CI에서 강제”는 “테스트로 구현했고 현재는 로컬 검증”으로 바꾼다.
- [ ] 적합성을 전면 자동화했다는 표현은 “계좌 적격성·위험자산 한도 규칙을 자동 검사”로 범위를 명시한다.
- [ ] “엔진·RAG 병렬 실행” 표현을 제거한다. 실제 질문 처리는 인텐트에 따라 순차 분기하며, 인증 컨텍스트 DB 조회만 병렬이다.
- [ ] “엔진 검증 답변”보다 **“검증된 결정론 답변”**이라고 표현한다.
- [ ] 3%·7%·9% 입력은 예측값이 아니라 비교 가정임을 화면·발표에서 일관되게 설명한다.
- [ ] `AGENTS.md`의 가드 미탐 대기 설명과 `아키텍처.md`의 보강 완료 설명이 현재 코드 상태와 맞는지 정합화한다.

## 10. 근거가 부족한 질문에 답하는 원칙

- 비교 벤치마크가 없으면 “A보다 B가 좋다” 대신 “우리 요구사항에서 B의 이 특성을 사용했다”고 답한다.
- 수치가 있으면 측정 환경·표본·기준일을 함께 말한다. 내부 실측을 운영 성능이나 일반적 모델 우위로 확대하지 않는다.
- 구현되지 않은 기능은 현재 범위와 다음 검증 방법을 함께 말한다.
- 금융 규칙은 기억으로 답하지 않고 코드·공식 근거·기준일을 확인한다.
- 질문의 전제가 틀리면 바로잡는다. 특히 LLM 계산, 엔진·RAG 병렬 실행, 과거 수익률 기반 추천이라는 전제는 사실과 다르다.
